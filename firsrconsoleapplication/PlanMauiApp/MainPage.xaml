<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PlanMauiApp.MainPage"
             xmlns:local="clr-namespace:PlanMauiApp">

    <ContentPage.BindingContext>
        <local:MainViewModel />
    </ContentPage.BindingContext>

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <Label Text="Wybierz date do wyświetenia"/>
            <HorizontalStackLayout>
                <Picker ItemsSource="{Binding PlannedYears}" Title="Rok" SelectedItem="{Binding SelectedYear}"/>
                <Picker ItemsSource="{Binding PlannedMonths}" Title="Miesiąc" SelectedItem="{Binding SelectedMonth}"/>
                <Picker ItemsSource="{Binding PlannedDays}" Title="Dzień" SelectedItem="{Binding SelectedDay}"/>
            </HorizontalStackLayout>

            <HorizontalStackLayout>
                <Label Text="Rok:"/>
                <Entry Text="{Binding EnteredYear}"/>
                <Label Text="Miesiąc:"/>
                <Entry Text="{Binding EnteredMonth}"/>
                <Label Text="Dzień:"/>
                <Entry Text="{Binding EnteredDay}"/>
            </HorizontalStackLayout>


        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
